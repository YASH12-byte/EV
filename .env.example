# Copy this file to .env locally. Never commit .env (it is gitignored).
# On Render / Railway, set these as dashboard environment variables instead.

SECRET_KEY=change-this-to-a-long-random-string
JWT_SECRET_KEY=change-this-to-another-long-random-string
FLASK_DEBUG=0
# DATABASE_URL=sqlite:///backend/ev_market.db
